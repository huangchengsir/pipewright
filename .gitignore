# Go
/devopstool
/devopstool-*
*.test
*.out
*.db
*.db-journal
*.db-wal
*.db-shm

# Frontend
/web/node_modules/
# 注:/web/dist 为 go:embed 所需,保留占位 index.html;真实产物(带 hash 的 assets)由 vite build 生成,不入库(避免每次构建 churn)。
/web/dist/assets/

# Claude Code 本地设置(机器特定权限/覆盖,不入库)
.claude/settings.local.json

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# sky-aitest（自动注入：工作区/凭证/运行时配置不入库）
# 注:这些规则全部锚定根目录(前导 /),避免裸目录名误伤源码(如 auth/ 曾误吞 internal/auth)。
.sky-aitest/
/auth/
.mcp.json
/seed.spec.ts
/runs/
/results/
/test-results/
