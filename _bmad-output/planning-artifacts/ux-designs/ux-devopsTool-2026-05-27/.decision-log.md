# Decision Log — ux-devopsTool-2026-05-27

UX 设计的规范化记忆与审计轨迹。

## 2026-05-27
- **意图**:Create。无既有 UX。
- **来源(by reference,不复制内容)**:
  - PRD `../../prds/prd-devopsTool-2026-05-27/`(FR-1~26,k8s 已延后)
  - 架构 `../../architecture.md`(前端约束:Vue3 + Naive UI + Pinia + Vue Router + Monaco 只读懒加载 + SSE 实时流;单管理员无 RBAC;双运行单二进制)
- **重点体验(用户指定)**:AI 失败诊断(FR-22 根因假说+证据+置信度)· 成功/失败差异对比视图(FR-25)· "干净直观"反击 Jenkins。
- **节奏**:用户选 **快车道**,"demo 做好就行" —— 视觉决定委托给设计方(用户保留否决权),产物 = 可截图 HTML demo 屏。
- **视觉方向(设计方定,待用户在英雄屏上确认)**:「冷静的工程控制台」—— 深色为主(契合 ops 低光/凌晨场景 + 与 Jenkins 拉开差距;浅色作令牌切换保留)· 层叠深色表面做深度 · **电青(cyan)单一品牌色** + 语义状态色(绿/琥珀/红)· 无衬线 + 等宽配对 · 强字阶对比。
- **策略**:先建英雄屏(失败诊断 + 差异对比)确认方向,再同方向铺其余屏。
- **视觉迭代(多轮)**:英雄屏经 v1→v6 收敛(v6 = 锚点);用户反馈逐步加细。已建页面集:运行详情/诊断、概览、项目/流水线配置、服务器、通知、登录、新建项目(AI 向导)。代码浏览/diff(FR-4)、各类表单弹窗待补。
- **两条全局视觉规则(用户反馈,写入 DESIGN.md)**:① **内容加宽**(主区 max-width ~1680,避免宽屏两侧大片留白/"左右太空");② **禁用 AI 套路图标**(✦ 星芒等"AI 味"符号),AI 功能改用克制的工程/诊断感标记(活动脉冲线)+ 青色语义,不靠星芒卖弄。
- **mock 文件**:`.working/mock-*.html`(run-diagnosis-v6 = 锚点;overview / project-pipeline / servers / notifications / login / new-project)。
- **用户视觉脑暴(第2轮)**:① 要 **专业工程感 + 现代感 + 动效感**(motion 为一等设计要素);② 暂无参照产品;③ **明暗双主题都要**;④ **要惊艳**(开源首页可截图水准)。
- **据此细化方向**:深色为默认、浅色为对等主题(双令牌);动效贯穿(诊断卡入场、置信度环填充、状态时间线、运行中脉冲、hover 抬升、diff 高亮扫过),仅用 transform/opacity 等合成器友好属性 + `prefers-reduced-motion` 降级。字体意图:Inter(无衬线)+ JetBrains Mono(等宽)。强调色:电青。

## 2026-05-28
- **意图**:Update —— 续做"待补 demo"(待办 A),沿用 v6 锚点视觉语言,不重定方向。
- **新增 7 张 demo**(`.working/`,均双主题 + 合成器友好动效 + `prefers-reduced-motion`):
  - `mock-runs.html` —— 全局运行列表:搜索 + 状态分段控件 + 项目/分支/触发下拉 + 时间范围;全宽 8 列表格,按日期分组,失败行红色渐变 + "查看诊断",含进行中(脉冲 + mini 进度条)/成功/已回滚多态 + 分页。
  - `mock-settings.html` —— 设置·AI 提供商(FR-24):设置二级导航(AI 提供商/凭据保险库/审计日志/账户/系统);提供商卡片(Claude 选中 / OpenAI / Ollama 零外发 / 自定义)、API Key 掩码 + 测试连接 + 延迟回显、模型选择、月度预算上限、诊断行为开关(自动诊断/挂知识库/日志外发默认关)、**诊断反馈闭环统计(FR-26)** 卡片(准确率/反馈数/环比 + 案例并入知识库说明)、未保存提示保存条。
  - `mock-settings-vault.html` —— 设置·凭据保险库 + 审计日志:6 条凭据(SSH/环境密钥/仓库账号/Git 令牌,图标分色)掩码值 + 类型 + 作用域 chip + 最近使用(闲置 60 天告警);审计日志时间线(谁/何时/对什么 + IP,新增/删除/读取/配置变更分色节点)。
  - `mock-code-diff.html` —— 代码浏览 + Diff(FR-4 Monaco 只读):提交头部(信息/作者/分支/±统计)、左文件树(目录缩进 + 每文件 ± 徽标 + 新文件标)、右统一 diff(GitHub 红绿、旧/新双行号槽、`@@` hunk 头 + 上下文说明、轻量语法着色、可折叠 hunk、AI"与失败相关"青色标注)。
  - `mock-run-live.html` —— 运行·进行中态:RUNNING 琥珀脉冲徽标 + 取消运行 + 自动滚动;流水线当前节点脉冲 + 连线流光;当前步骤进度卡(spinner + 百分比 + 流光进度条);实时日志终端(SSE 标识 + 步骤高亮行 + 闪烁光标);右侧步骤列表(✓/spinner/待开始)+ 运行信息(worker 队列)。
  - `mock-run-success.html` —— 运行·成功终态:SUCCESS 绿徽标;流水线全绿(节点带 ✓);成功英雄卡(绿渐变)+ **零停机切换 4 步流程图**(拉起新实例→健康门控→流量切换→旧实例下线)+ 4 项健康门控核对 + switch.log 终端;右侧"本次/上次"对比(环境变量全一致、文件变更、产物 +0.3MB)+ 运行信息(当前线上/可回滚)。
  - `mock-dialogs.html` —— 弹窗设计系统:暗化 scrim(+ 细网格纹理)背景上 2×2 展陈 4 个高频模态——添加服务器(SSH agentless + 从保险库选私钥 + 测试连接结果)、手动触发(分支/环境分段/KV 参数/跳过缓存)、添加凭据(类型/掩码值/作用域 chip 多选)、添加通知渠道(类型分段/Webhook/签名/仅失败推送/发送测试);统一模态语言:图标+标题/副标题、字段网格、底部"次按钮+主按钮"、测试类操作左下、危险操作色异化。
- **覆盖**:待办 A 的 ①设置 ②运行列表 ③代码diff ④运行进行中/成功态 ⑤关键弹窗 全部完成。页面集累计 13 张 mock。
- **核验受限**:本次 Playwright MCP 桥接扩展未连接,无法自动逐页截图;改为 `open` 在用户默认浏览器打开 7 张供肉眼审阅 + 写入时按锁定令牌静态自检。视觉细节(溢出/对齐)待用户确认。
- **下一步**:蒸馏 `DESIGN.md`(令牌+组件规格+Do/Don't)+ `EXPERIENCE.md`(IA+流程+状态),好 mock 提进 `mockups/`,跑收尾。
- **通知配置模型(用户明确)**:**继承 + 覆盖**。① 渠道连接 = 全局共享(只配一次);② 事件→渠道路由 + 内容模板 = **全局默认**,新流水线默认继承;③ 每个流水线可在自己的「结果通知」任务里切到「为本流水线自定义」,定义本流水线的事件路由(渠道仍取自全局已连接的)+ 可选覆盖模板/额外接收人。继承 vs 自定义为整体切换(自定义时整套替换该流水线的路由),非逐事件 merge。
  - 据此改两张 mock:`mock-notifications.html`(渠道标"全局共享"、路由/模板标"全局默认 · 可在流水线覆盖"、新增「流水线覆盖」概览:acme-web/payments-api=自定义,api-gateway/blog=沿用默认);`mock-project-pipeline.html`(选中「结果通知」任务,drawer = 范围分段「继承全局默认 ↔ 为本流水线自定义」+ 本流水线事件路由 chip 矩阵 + 模板来源 + 额外接收人)。
- **PRD 缺口核对(FR-1~26)+ 补 4 张**:逐条对照后发现 4 个真缺口(非状态),用户确认全做:
  - `mock-pipeline-triggers.html` —— pipeline「触发设置」tab(FR-1/FR-2):Webhook 接收端点 + 签名密钥(掩码/复制/重置)、触发事件勾选(push/tag/PR + 手动常开)、**分支(通配)→ 环境 + 目标服务器映射表**(main→生产 生产-1/2、release/*、dev→测试、feat/* 仅构建)。
  - `mock-service-logs.html` —— 服务日志查看器(FR-16):左服务选择器(按服务器分组)+ 实时/历史分段 + 关键词筛选高亮 + 级别着色(INFO/WARN/ERROR)+ 时间范围 + 下载/全屏 + 跟随尾部 + SSE 标识。
  - `mock-anomaly.html` —— 运行时异常检测(FR-23):按服务/框架的检测规则列表(重启循环/内存/5xx/健康/磁盘/P95,带开关与作用域)+ 采集设置(轮询间隔/去抖 + SSH 轮询 agentless 说明)+ 告警 feed(严重/警告/已恢复,已通知渠道,失败项带"AI 排查"入口)。
  - `mock-run-multi.html` —— 多机部署·部分失败(FR-13):一次构建并行下发 3 台,聚合"2 成功/1 失败回滚",每台独立状态卡(各自步骤+耗时,失败机健康检查超时→跳过切换→回滚 #128 + AI 诊断入口),强调结果独立互不连累 + 已携失败清单通知。
  - **PRD 覆盖**:FR-1~26 主要交付面均有可截图 demo。
- **完整度自检 + 补 4 张**(用户:"demo 肯定要完善"):对照"完整产品该有的界面"再查,补了 4 个独立界面:
  - `mock-onboarding.html` —— 首次使用/空状态:欢迎 hero + 3 步引导清单(连 AI ✓ / 加服务器(当前)/ 建项目(锁),带进度条)+ 价值主张三卡(≤100MB / 替三件套 / AI 诊断)+ 跳过引导。
  - `mock-projects.html` —— 项目列表:8 卡网格,每卡 = 名称/状态徽标/仓库分支/迷你流水线阶段点/上次运行(失败带 AI 摘要红条)/目标服务器/快捷(流水线·运行),失败卡红色描边、进行中琥珀 + 新建卡。
  - `mock-settings-system.html` —— 系统设置:实例信息(版本+升级/SQLite modernc 纯Go)、**资源占用(64/100MB 绿色预算条 = 轻量卖点)**、运行模式(原生二进制选中/Docker)、数据备份与迁移(每日备份/下载/导入)、审计 sink + secret 脱敏(AC-SEC-03/04)、危险区(导出/重置)。
  - `mock-settings-account.html` —— 账户设置:管理员身份(头像/用户名/邮箱/唯一管理员)、修改密码(强度条)、登录会话(当前设备 + 可撤销)、登录失败锁定/单管理员说明、登出。
  - **活跃 mock 共 22 张**(另留 run-diagnosis v1~v5 旧稿待删)。
- **横切状态做成规范页(用户:toast 之类也要一张页告诉前端规范)+ 第 23 张**:
  - `mock-states-components.html` —— 组件与状态规范(前端交接样式指南):① 状态徽标词汇(成功/失败/进行中/部分失败/已回滚/排队)② 按钮层级 + 交互态(default/hover/loading/disabled)③ Toast 4 型(成功/错误/警告/AI信息,右下堆叠/自动消失/行动)④ 内联 Banner 4 型 ⑤ 加载骨架(shimmer)⑥ 空状态 ⑦ 错误态(字段内联/整页重试/**AI 诊断不可用降级**/置信度标记)⑧ 二次确认(普通 + 高危输入名称确认)⑨ 表单控件各态(输入/开关/勾选/单选/分段/下拉)⑩ Tooltip ⑪ 进度/Spinner。每节右上标 token 用法 + 行为规范。
  - **活跃 mock 共 23 张**。
- **再自检补齐 pipeline 剩余两 tab(第 24、25 张)**:
  - `mock-pipeline-vars.html` —「变量与缓存」tab:**构建模型 A 自带 Dockerfile / B 工具链切换(FR-5)**、构建变量 KV 表(明文 + 从保险库引用的 secret 掩码)、依赖缓存(路径/键/命中率/大小/清除)、未匹配分支处理(记录/忽略)、默认跳过缓存开关。
  - `mock-pipeline-env.html` —「环境与凭据」tab:环境定义(生产/测试 + 各自目标服务器 + 环境变量,secret 标保险库)、**本流水线引用的保险库凭据(仓库令牌/部署SSH/镜像仓库账号,FR-3 引用绑定)**、**镜像仓库绑定 Harbor(FR-7)**+ 连通性。
  - **活跃 mock 共 25 张**。pipeline 4 个 tab(编排/变量与缓存/触发设置/环境与凭据)全部成图;FR-1~26 + AC-SEC + 实例管理 + 通用状态规范全覆盖。**demo 阶段彻底完成,IA 内无任何空白屏。** 真正只余写文字的状态变体(服务器分组、仅构建无部署运行)与可在现有图基础上文字说明的项。
- **进入 Finalize · 蒸馏两份脊柱(2026-05-28)**:25 张 mock 从 `.working/` 提进 `mockups/`(run-diagnosis-v6 → 重命名 mock-run-diagnosis.html);并行子代理蒸馏:
  - `DESIGN.md`(status: final)—— 锚点 `mockups/mock-run-diagnosis.html` 取双主题完整 OKLCH 令牌;frontmatter 填 colors(56)/typography(9 角色)/rounded(7)/spacing(11,含 content-max 1680)/components(19);正文锁定顺序 Brand&Style→Colors→Typography→Layout&Spacing→Elevation→Shapes→Components→Do/Don't;Do/Don't 收录两条全局规则 + 「避免 AI 味」。
  - `EXPERIENCE.md`(status: final)—— Foundation(Web/单管理员/Vue3+Naive UI/SSE/Monaco)· 完整 IA(rail + 二级 + pipeline 4 tab + 运行 5 态 + 设置 4 项,FR-1~26 全映射 + inline mock 链接)· Voice&Tone · Component/State Patterns(含 AI 降级)· Interaction Primitives · Accessibility Floor(WCAG 2.2 AA)· 产品特定模型(通知继承+覆盖、AI 诊断旁路)· 4 条具名主角(黄佳伟)旅程含高潮节拍。
  - 一致性修正:EXPERIENCE 的令牌引用 `{color.*}` → `{colors.*}` 对齐 DESIGN frontmatter。
  - 两份均声明「脊柱在与 mock 冲突时为准」。DESIGN 列出留白项(无 px 字阶、无 focus-visible 规格、无动效时长/缓动令牌集、无 z-index 层级);均非阻塞。
  - **剩余 Finalize 步**:(可选)Reviewer Gate 评审 → 开放项三级分诊 → status 已设 final(如需评审可回退)→ 跑 doc 标准(editorial review)。
- **UX 封板(2026-05-28)**:用户选择**跳过 Reviewer Gate 与 editorial 精修**,直接收尾。DESIGN.md / EXPERIENCE.md = final,25 张 mock 在 `mockups/`,决策轨迹完整。**UX 阶段完成。** 下一 BMad 步:`bmad-create-epics-and-stories`(架构已 complete,无需 architecture 步)。
- **布局修正(用户反馈:宽屏右边空一大截)**:三张 pipeline 子 tab 页(triggers/vars/env)原 `.content` 设了 `max-width` 且靠左,宽屏右侧留大片空。改为 **content 铺满宽度**(与全宽顶部栏/tab 左右对齐);其中 vars/env 这类表单页再拆为**两栏**(vars:构建变量 | 缓存+其它;env:凭据 | 镜像仓库),避免单列拉太宽显空。符合「内容铺满、避免左右太空」全局规则。注:settings 系列为「左二级导航 + 表单列」的标准布局(GitHub/Vercel 同款),右侧留白属有意,如需也可加宽。
